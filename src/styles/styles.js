import { StyleSheet } from 'react-native'

export default styles = StyleSheet.create({
  position_absolute_full: {
    position: 'absolute',
    top: 0,
    bottom: 0,
    right: 0,
    left: 0
  },
})